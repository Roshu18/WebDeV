7. Write a C program to implementand demonstrate the Selection sort
 algorithm.
 #include <stdio.h>
 #include <conio.h>
 void printarray(int *a,int n)
 {
 int i;
 for(i=0;i<n;i++)
{
 printf("%4d",a[i]);
 }}
 void selectionsort(int *a,int size)
 {
 int i,j,k,imin;
 int start;
 start = a[0];
 for(i=0;i<size-1;i++)
 {
 imin=i; // get the index of minimum data
 for(j=i+1;j<size;j++)
 if(a[j]<a[imin])
 imin=j;
 // placing in correct position
 int temp;
 temp=a[i];
 a[i]=a[imin];
 a[imin]=temp;
 printf("\nLoop %d: ",i);
 for(k=0;k<size;k++)
 {
 printf(" %4d",a[k]);
 }}}
 void main()
 {
 int n,i;
 int a[]={12,19,55,2};
 clrscr();
 n=sizeof(a)/sizeof(a[0]);
 printf("\n the size of array is : %d",n);
 printf ("\n Array before sorting: ");
 printarray(a,n);
 selectionsort(a,n);
 printf("\n Array after sorting");
 printarray(a,n);
 getch();
 }